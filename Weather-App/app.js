
function calculateTaxes(price, quantity) {
    var salesTax = .10;
    var totalPrice;
    // Only change code below this line
    totalPrice = salesTax * (price * quantity);
    

    // Only change code above this line
    return totalPrice;
}

// Change the inputs below to test your code
calculateTaxes(1, 10);


